/**
* Rules that match tiles that move temps.
*/ 

@Include "./Move_Load.jburg"

/*
@Include "./Move_Store.jburg"

move = MOVE(temp dst, CONST(void) src) : 1
{
	emitter.moveConstToTemp(dst, ((Tree.CONST)src).value);
	return null;
}

move = MOVE(temp dst, exp src) : 1
{
	emitter.moveExpToTemp(dst, src);
	return null;
}
*/