/**
* Rules that match tiles that move temps.
*/ 

@Include "./Move_Load.jburg"

@Include "./Move_Store.jburg"

move = MOVE(temp dst, integerConstant src) : 10
@Reduction reducer.move(__p, dst, src);

move = MOVE(temp dst, exp src) : 10
@Reduction reducer.move(__p, dst, src);