call = CALL(exp args*) : 1
{
    Tree.CALL call = ((Tree.CALL)#call);
    for(var temp : args) {
        //if i <= 6 goes to regiser

        //if i > 6 goes on stack.
        System.out.println("add instructions to move " + temp + " correct place");
    }
    System.out.println("call()");
    return null;
}