move = MOVE(MEM(temp t1), temp t2) : 1
{
    System.out.println("movq (" + t1 + "), " + t2);
	return null;
}

move = MOVE(MEM(exp dst), MEM(exp src)) : 1
{
	System.out.println("move store statment");

	return null;
}

move = MOVE(exp t2, MEM(BINOP(con left, exp right))) : 1
{
	System.out.println("move store statment");
	return null;
}
